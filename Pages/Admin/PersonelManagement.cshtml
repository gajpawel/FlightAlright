@page
@model FlightAlright.Pages.Admin.PersonelManagementModel
@{
    ViewData["Title"] = "Zarządzanie personelem";
}

@functions {
    string GetActiveClass(string pageName)
    {
        return ViewContext.RouteData.Values["Page"].ToString().EndsWith(pageName) ? "active" : "";
    }
}

<div class="row">
    <div class="col-md-auto">
        <div class="admin-menu">
            <a asp-page="./AdminProfile" class="@GetActiveClass("/AdminProfile")">Konto</a>
            <a asp-page="./PersonelManagement" class="@GetActiveClass("/PersonelManagement")">Personel</a>
            <a asp-page="./AirportManagement" class="@GetActiveClass("/AirportManagement")">Lotniska</a>
            <a asp-page="./FleetManagement" class="@GetActiveClass("/FleetManagement")">Flota</a>
            <a asp-page="./FlightManagement" class="@GetActiveClass("/FlightManagement")">Loty</a>
            <a asp-page="/Admin/Reports/Reports" class="@GetActiveClass("/Reports/Reports")">Raporty</a>
        </div>
    </div>

    <div class="col bg-light p-4 rounded shadow-sm">
        <h2>Zarządzanie personelem</h2>
        <p>W tym panelu możesz dodawać nowych pracowników oraz przeglądać istniejących.</p>

        <a class="btn btn-danger mb-3" asp-page="/Admin/AddAdmin">➕ Dodaj nowego administratora</a><br />
        <a class="btn btn-primary mb-3" asp-page="/Employees/Create">➕ Dodaj nowego pracownika</a><br />
        <a class="btn btn-secondary mb-3" asp-page="/Employees/Index">👥 Zobacz listę pracowników</a><br />
        <a class="btn btn-outline-dark" asp-page="/Admin/PositionManagement">🏷️ Zarządzaj stanowiskami</a>
    </div>
</div>

<style>
    .admin-menu {
        display: flex;
        flex-direction: column;
        gap: .5rem;
    }

        .admin-menu a.active {
            font-weight: 600;
            text-decoration: underline;
        }
</style>